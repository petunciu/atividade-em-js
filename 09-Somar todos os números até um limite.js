function somarAte(limite) {
  let soma = 0;

  for (let i = 1; i <= limite; i++) {
    soma += i;
  }

  return soma;
}

// Exemplo de uso:
console.log(somarAte(5));  
5 = 15
console.log(somarAte(10)); 
